import { AddTotalEntryPipe } from './add-total-entry.pipe';

describe('AddTotalEntryPipe', () => {
  it('create an instance', () => {
    const pipe = new AddTotalEntryPipe();
    expect(pipe).toBeTruthy();
  });
});
